diff --git a/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java b/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java
index d9ad01e..e5a4cb7 100644
--- a/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java
+++ b/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java
@@ -77,6 +77,7 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
     private final Map<String, PluginCall> pendingDurationCalls = new HashMap<>();
 
     // Notification center support
+    private boolean backgroundPlaybackEnabled = false;
     private boolean showNotification = false;
     private Map<String, Map<String, String>> notificationMetadataMap = new HashMap<>();
     private MediaSessionCompat mediaSession;
@@ -133,6 +134,10 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
     protected void handleOnPause() {
         super.handleOnPause();
 
+        if (backgroundPlaybackEnabled) {
+            return;
+        }
+
         try {
             if (audioAssetList != null) {
                 for (HashMap.Entry<String, AudioAsset> entry : audioAssetList.entrySet()) {
@@ -156,6 +161,10 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
     protected void handleOnResume() {
         super.handleOnResume();
 
+        if (backgroundPlaybackEnabled) {
+            return;
+        }
+
         try {
             if (resumeList != null) {
                 while (!resumeList.isEmpty()) {
@@ -187,6 +196,7 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
 
         boolean focus = call.getBoolean(OPT_FOCUS_AUDIO, false);
         boolean background = call.getBoolean("background", false);
+        this.backgroundPlaybackEnabled = background;
         this.fadeMusic = call.getBoolean("fade", false);
         this.showNotification = call.getBoolean(SHOW_NOTIFICATION, false);
 
@@ -269,59 +279,72 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
 
     @PluginMethod
     public void getCurrentTime(final PluginCall call) {
-        try {
-            initSoundPool();
+        this.getActivity().runOnUiThread(
+            new Runnable() {
+                @Override
+                public void run() {
+                    try {
+                        initSoundPool();
 
-            String audioId = call.getString(ASSET_ID);
+                        String audioId = call.getString(ASSET_ID);
 
-            if (!isStringValid(audioId)) {
-                call.reject(ERROR_AUDIO_ID_MISSING + " - " + audioId);
-                return;
-            }
+                        if (!isStringValid(audioId)) {
+                            call.reject(ERROR_AUDIO_ID_MISSING + " - " + audioId);
+                            return;
+                        }
 
-            if (audioAssetList.containsKey(audioId)) {
-                AudioAsset asset = audioAssetList.get(audioId);
-                if (asset != null) {
-                    call.resolve(new JSObject().put("currentTime", asset.getCurrentPosition()));
+                        if (audioAssetList.containsKey(audioId)) {
+                            AudioAsset asset = audioAssetList.get(audioId);
+                            if (asset != null) {
+                                call.resolve(new JSObject().put("currentTime", asset.getCurrentPosition()));
+                            }
+                        } else {
+                            call.reject(ERROR_AUDIO_ASSET_MISSING + " - " + audioId);
+                        }
+                    } catch (Exception ex) {
+                        call.reject(ex.getMessage());
+                    }
                 }
-            } else {
-                call.reject(ERROR_AUDIO_ASSET_MISSING + " - " + audioId);
             }
-        } catch (Exception ex) {
-            call.reject(ex.getMessage());
-        }
+        );
     }
 
     @PluginMethod
-    public void getDuration(PluginCall call) {
-        try {
-            String audioId = call.getString(ASSET_ID);
-            if (!isStringValid(audioId)) {
-                call.reject(ERROR_AUDIO_ID_MISSING + " - " + audioId);
-                return;
-            }
+    public void getDuration(final PluginCall call) {
+        this.getActivity().runOnUiThread(
+            new Runnable() {
+                @Override
+                public void run() {
+                    try {
+                        String audioId = call.getString(ASSET_ID);
+                        if (!isStringValid(audioId)) {
+                            call.reject(ERROR_AUDIO_ID_MISSING + " - " + audioId);
+                            return;
+                        }
 
-            if (audioAssetList.containsKey(audioId)) {
-                AudioAsset asset = audioAssetList.get(audioId);
-                if (asset != null) {
-                    double duration = asset.getDuration();
-                    if (duration > 0) {
-                        JSObject ret = new JSObject();
-                        ret.put("duration", duration);
-                        call.resolve(ret);
-                    } else {
-                        // Save the call to resolve it later when duration is available
-                        saveDurationCall(audioId, call);
+                        if (audioAssetList.containsKey(audioId)) {
+                            AudioAsset asset = audioAssetList.get(audioId);
+                            if (asset != null) {
+                                double duration = asset.getDuration();
+                                if (duration > 0) {
+                                    JSObject ret = new JSObject();
+                                    ret.put("duration", duration);
+                                    call.resolve(ret);
+                                } else {
+                                    saveDurationCall(audioId, call);
+                                }
+                            } else {
+                                call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                            }
+                        } else {
+                            call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                        }
+                    } catch (Exception ex) {
+                        call.reject(ex.getMessage());
                     }
-                } else {
-                    call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
                 }
-            } else {
-                call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
             }
-        } catch (Exception ex) {
-            call.reject(ex.getMessage());
-        }
+        );
     }
 
     @PluginMethod
@@ -337,64 +360,76 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
     }
 
     @PluginMethod
-    public void pause(PluginCall call) {
-        try {
-            initSoundPool();
-            String audioId = call.getString(ASSET_ID);
+    public void pause(final PluginCall call) {
+        this.getActivity().runOnUiThread(
+            new Runnable() {
+                @Override
+                public void run() {
+                    try {
+                        initSoundPool();
+                        String audioId = call.getString(ASSET_ID);
 
-            if (audioAssetList.containsKey(audioId)) {
-                AudioAsset asset = audioAssetList.get(audioId);
-                if (asset != null) {
-                    boolean wasPlaying = asset.pause();
+                        if (audioAssetList.containsKey(audioId)) {
+                            AudioAsset asset = audioAssetList.get(audioId);
+                            if (asset != null) {
+                                boolean wasPlaying = asset.pause();
 
-                    if (wasPlaying) {
-                        resumeList.add(asset);
-                    }
+                                if (wasPlaying) {
+                                    resumeList.add(asset);
+                                }
 
-                    // Update notification when paused
-                    if (showNotification) {
-                        updatePlaybackState(PlaybackStateCompat.STATE_PAUSED);
-                    }
+                                if (showNotification) {
+                                    updatePlaybackState(PlaybackStateCompat.STATE_PAUSED);
+                                }
 
-                    call.resolve();
-                } else {
-                    call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                                call.resolve();
+                            } else {
+                                call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                            }
+                        } else {
+                            call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                        }
+                    } catch (Exception ex) {
+                        call.reject(ex.getMessage());
+                    }
                 }
-            } else {
-                call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
             }
-        } catch (Exception ex) {
-            call.reject(ex.getMessage());
-        }
+        );
     }
 
     @PluginMethod
-    public void resume(PluginCall call) {
-        try {
-            initSoundPool();
-            String audioId = call.getString(ASSET_ID);
+    public void resume(final PluginCall call) {
+        this.getActivity().runOnUiThread(
+            new Runnable() {
+                @Override
+                public void run() {
+                    try {
+                        initSoundPool();
+                        String audioId = call.getString(ASSET_ID);
 
-            if (audioAssetList.containsKey(audioId)) {
-                AudioAsset asset = audioAssetList.get(audioId);
-                if (asset != null) {
-                    asset.resume();
-                    resumeList.add(asset);
+                        if (audioAssetList.containsKey(audioId)) {
+                            AudioAsset asset = audioAssetList.get(audioId);
+                            if (asset != null) {
+                                asset.resume();
+                                resumeList.add(asset);
 
-                    // Update notification when resumed
-                    if (showNotification) {
-                        updatePlaybackState(PlaybackStateCompat.STATE_PLAYING);
-                    }
+                                if (showNotification) {
+                                    updatePlaybackState(PlaybackStateCompat.STATE_PLAYING);
+                                }
 
-                    call.resolve();
-                } else {
-                    call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                                call.resolve();
+                            } else {
+                                call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                            }
+                        } else {
+                            call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
+                        }
+                    } catch (Exception ex) {
+                        call.reject(ex.getMessage());
+                    }
                 }
-            } else {
-                call.reject(ERROR_ASSET_NOT_LOADED + " - " + audioId);
             }
-        } catch (Exception ex) {
-            call.reject(ex.getMessage());
-        }
+        );
     }
 
     @PluginMethod
@@ -504,29 +539,36 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
 
     @PluginMethod
     public void isPlaying(final PluginCall call) {
-        try {
-            initSoundPool();
+        this.getActivity().runOnUiThread(
+            new Runnable() {
+                @Override
+                public void run() {
+                    try {
+                        initSoundPool();
 
-            String audioId = call.getString(ASSET_ID);
+                        String audioId = call.getString(ASSET_ID);
 
-            if (!isStringValid(audioId)) {
-                call.reject(ERROR_AUDIO_ID_MISSING + " - " + audioId);
-                return;
-            }
+                        if (!isStringValid(audioId)) {
+                            call.reject(ERROR_AUDIO_ID_MISSING + " - " + audioId);
+                            return;
+                        }
 
-            if (audioAssetList.containsKey(audioId)) {
-                AudioAsset asset = audioAssetList.get(audioId);
-                if (asset != null) {
-                    call.resolve(new JSObject().put("isPlaying", asset.isPlaying()));
-                } else {
-                    call.reject(ERROR_AUDIO_ASSET_MISSING + " - " + audioId);
+                        if (audioAssetList.containsKey(audioId)) {
+                            AudioAsset asset = audioAssetList.get(audioId);
+                            if (asset != null) {
+                                call.resolve(new JSObject().put("isPlaying", asset.isPlaying()));
+                            } else {
+                                call.reject(ERROR_AUDIO_ASSET_MISSING + " - " + audioId);
+                            }
+                        } else {
+                            call.reject(ERROR_AUDIO_ASSET_MISSING + " - " + audioId);
+                        }
+                    } catch (Exception ex) {
+                        call.reject(ex.getMessage());
+                    }
                 }
-            } else {
-                call.reject(ERROR_AUDIO_ASSET_MISSING + " - " + audioId);
             }
-        } catch (Exception ex) {
-            call.reject(ex.getMessage());
-        }
+        );
     }
 
     @PluginMethod
@@ -929,45 +971,66 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
             new MediaSessionCompat.Callback() {
                 @Override
                 public void onPlay() {
-                    if (currentlyPlayingAssetId != null && audioAssetList.containsKey(currentlyPlayingAssetId)) {
-                        AudioAsset asset = audioAssetList.get(currentlyPlayingAssetId);
-                        try {
-                            if (asset != null && !asset.isPlaying()) {
-                                asset.resume();
-                                updatePlaybackState(PlaybackStateCompat.STATE_PLAYING);
+                    getActivity().runOnUiThread(
+                        new Runnable() {
+                            @Override
+                            public void run() {
+                                if (currentlyPlayingAssetId != null && audioAssetList.containsKey(currentlyPlayingAssetId)) {
+                                    AudioAsset asset = audioAssetList.get(currentlyPlayingAssetId);
+                                    try {
+                                        if (asset != null) {
+                                            asset.resume();
+                                            updatePlaybackState(PlaybackStateCompat.STATE_PLAYING);
+                                        }
+                                    } catch (Exception e) {
+                                        Log.e(TAG, "Error resuming audio from media session", e);
+                                    }
+                                }
                             }
-                        } catch (Exception e) {
-                            Log.e(TAG, "Error resuming audio from media session", e);
                         }
-                    }
+                    );
                 }
 
                 @Override
                 public void onPause() {
-                    if (currentlyPlayingAssetId != null && audioAssetList.containsKey(currentlyPlayingAssetId)) {
-                        AudioAsset asset = audioAssetList.get(currentlyPlayingAssetId);
-                        try {
-                            if (asset != null) {
-                                asset.pause();
-                                updatePlaybackState(PlaybackStateCompat.STATE_PAUSED);
+                    getActivity().runOnUiThread(
+                        new Runnable() {
+                            @Override
+                            public void run() {
+                                if (currentlyPlayingAssetId != null && audioAssetList.containsKey(currentlyPlayingAssetId)) {
+                                    AudioAsset asset = audioAssetList.get(currentlyPlayingAssetId);
+                                    try {
+                                        if (asset != null) {
+                                            asset.pause();
+                                            updatePlaybackState(PlaybackStateCompat.STATE_PAUSED);
+                                        }
+                                    } catch (Exception e) {
+                                        Log.e(TAG, "Error pausing audio from media session", e);
+                                    }
+                                }
                             }
-                        } catch (Exception e) {
-                            Log.e(TAG, "Error pausing audio from media session", e);
                         }
-                    }
+                    );
                 }
 
                 @Override
                 public void onStop() {
-                    if (currentlyPlayingAssetId != null) {
-                        try {
-                            stopAudio(currentlyPlayingAssetId);
-                            clearNotification();
-                            currentlyPlayingAssetId = null;
-                        } catch (Exception e) {
-                            Log.e(TAG, "Error stopping audio from media session", e);
+                    getActivity().runOnUiThread(
+                        new Runnable() {
+                            @Override
+                            public void run() {
+                                if (currentlyPlayingAssetId != null) {
+                                    try {
+                                        stopAudio(currentlyPlayingAssetId);
+                                        clearNotification();
+                                        currentlyPlayingAssetId = null;
+                                    } catch (Exception e) {
+                                        Log.e(TAG, "Error stopping audio from media session", e);
+                                    }
+                                }
+                            }
                         }
-                    }
+                    );
                 }
             }
         );
@@ -1047,11 +1110,37 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
         }
     }
 
+    public void updateMediaSessionProgress(double currentTimeSec) {
+        if (!showNotification || mediaSession == null || currentlyPlayingAssetId == null) return;
+
+        long positionMs = Math.max(0, Math.round(currentTimeSec * 1000));
+        PlaybackStateCompat current = mediaSession.getController().getPlaybackState();
+        int state = current != null ? current.getState() : PlaybackStateCompat.STATE_PLAYING;
+        float speed = state == PlaybackStateCompat.STATE_PLAYING ? 1.0f : 0.0f;
+
+        PlaybackStateCompat.Builder stateBuilder = new PlaybackStateCompat.Builder()
+            .setState(state, positionMs, speed)
+            .setActions(PlaybackStateCompat.ACTION_PLAY | PlaybackStateCompat.ACTION_PAUSE | PlaybackStateCompat.ACTION_STOP);
+        mediaSession.setPlaybackState(stateBuilder.build());
+    }
+
     private void updatePlaybackState(int state) {
         if (mediaSession == null) return;
 
+        long positionMs = 0;
+        if (currentlyPlayingAssetId != null && audioAssetList.containsKey(currentlyPlayingAssetId)) {
+            AudioAsset asset = audioAssetList.get(currentlyPlayingAssetId);
+            if (asset != null) {
+                try {
+                    positionMs = Math.max(0, Math.round(asset.getCurrentPosition() * 1000));
+                } catch (Exception e) {
+                    Log.e(TAG, "Error getting position for playback state", e);
+                }
+            }
+        }
+
         PlaybackStateCompat.Builder stateBuilder = new PlaybackStateCompat.Builder()
-            .setState(state, 0, state == PlaybackStateCompat.STATE_PLAYING ? 1.0f : 0.0f)
+            .setState(state, positionMs, state == PlaybackStateCompat.STATE_PLAYING ? 1.0f : 0.0f)
             .setActions(PlaybackStateCompat.ACTION_PLAY | PlaybackStateCompat.ACTION_PAUSE | PlaybackStateCompat.ACTION_STOP);
         mediaSession.setPlaybackState(stateBuilder.build());
     }
diff --git a/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/RemoteAudioAsset.java b/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/RemoteAudioAsset.java
index adc1951..e4ba8c9 100644
--- a/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/RemoteAudioAsset.java
+++ b/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/RemoteAudioAsset.java
@@ -19,6 +19,9 @@ import androidx.media3.exoplayer.source.ProgressiveMediaSource;
 import java.io.File;
 import java.util.ArrayList;
 import java.util.Map;
+import java.util.concurrent.Callable;
+import java.util.concurrent.CountDownLatch;
+import java.util.concurrent.TimeUnit;
 
 @UnstableApi
 public class RemoteAudioAsset extends AudioAsset {
@@ -149,6 +152,38 @@ public class RemoteAudioAsset extends AudioAsset {
         Log.d(TAG, "Player initialization complete");
     }
 
+    private <T> T runOnMainSync(Callable<T> callable, T fallback) throws Exception {
+        if (Looper.myLooper() == Looper.getMainLooper()) {
+            return callable.call();
+        }
+
+        final Object[] result = new Object[] { fallback };
+        final Exception[] error = new Exception[1];
+        final CountDownLatch latch = new CountDownLatch(1);
+        owner
+            .getActivity()
+            .runOnUiThread(
+                new Runnable() {
+                    @Override
+                    public void run() {
+                        try {
+                            result[0] = callable.call();
+                        } catch (Exception e) {
+                            error[0] = e;
+                        } finally {
+                            latch.countDown();
+                        }
+                    }
+                }
+            );
+
+        latch.await(2, TimeUnit.SECONDS);
+        if (error[0] != null) {
+            throw error[0];
+        }
+        return (T) result[0];
+    }
+
     private String getStateString(int state) {
         switch (state) {
             case Player.STATE_IDLE:
@@ -229,23 +264,23 @@ public class RemoteAudioAsset extends AudioAsset {
 
     @Override
     public boolean pause() throws Exception {
-        final boolean[] wasPlaying = { false };
-        owner
-            .getActivity()
-            .runOnUiThread(
-                new Runnable() {
-                    @Override
-                    public void run() {
-                        for (ExoPlayer player : players) {
-                            if (player.isPlaying()) {
-                                player.pause();
-                                wasPlaying[0] = true;
-                            }
+        return runOnMainSync(
+            new Callable<Boolean>() {
+                @Override
+                public Boolean call() {
+                    boolean wasPlaying = false;
+                    for (ExoPlayer player : players) {
+                        if (player.isPlaying()) {
+                            player.pause();
+                            wasPlaying = true;
                         }
                     }
+                    stopCurrentTimeUpdates();
+                    return wasPlaying;
                 }
-            );
-        return wasPlaying[0];
+            },
+            false
+        );
     }
 
     @Override
@@ -373,21 +408,27 @@ public class RemoteAudioAsset extends AudioAsset {
     public boolean isPlaying() throws Exception {
         if (players.isEmpty() || !isPrepared) return false;
 
-        ExoPlayer player = players.get(playIndex);
-        return player != null && player.isPlaying();
+        return runOnMainSync(
+            new Callable<Boolean>() {
+                @Override
+                public Boolean call() {
+                    ExoPlayer player = players.get(playIndex);
+                    return player != null && player.isPlaying();
+                }
+            },
+            false
+        );
     }
 
     @Override
     public double getDuration() {
         Log.d(TAG, "getDuration called, players empty: " + players.isEmpty() + ", isPrepared: " + isPrepared);
         if (!players.isEmpty() && isPrepared) {
-            final double[] duration = { 0 };
-            owner
-                .getActivity()
-                .runOnUiThread(
-                    new Runnable() {
+            try {
+                return runOnMainSync(
+                    new Callable<Double>() {
                         @Override
-                        public void run() {
+                        public Double call() {
                             ExoPlayer player = players.get(playIndex);
                             int state = player.getPlaybackState();
                             Log.d(TAG, "Player state: " + state + " (READY=" + Player.STATE_READY + ")");
@@ -395,18 +436,19 @@ public class RemoteAudioAsset extends AudioAsset {
                                 long rawDuration = player.getDuration();
                                 Log.d(TAG, "Raw duration: " + rawDuration + ", TIME_UNSET=" + androidx.media3.common.C.TIME_UNSET);
                                 if (rawDuration != androidx.media3.common.C.TIME_UNSET) {
-                                    duration[0] = rawDuration / 1000.0;
-                                    Log.d(TAG, "Final duration in seconds: " + duration[0]);
-                                } else {
-                                    Log.d(TAG, "Duration is TIME_UNSET");
+                                    double durationSec = rawDuration / 1000.0;
+                                    Log.d(TAG, "Final duration in seconds: " + durationSec);
+                                    return durationSec;
                                 }
-                            } else {
-                                Log.d(TAG, "Player not in READY state");
                             }
+                            return 0.0;
                         }
-                    }
+                    },
+                    0.0
                 );
-            return duration[0];
+            } catch (Exception e) {
+                Log.e(TAG, "Error getting duration", e);
+            }
         }
         Log.d(TAG, "No players or not prepared for duration");
         return 0;
@@ -415,23 +457,25 @@ public class RemoteAudioAsset extends AudioAsset {
     @Override
     public double getCurrentPosition() {
         if (!players.isEmpty() && isPrepared) {
-            final double[] position = { 0 };
-            owner
-                .getActivity()
-                .runOnUiThread(
-                    new Runnable() {
+            try {
+                return runOnMainSync(
+                    new Callable<Double>() {
                         @Override
-                        public void run() {
+                        public Double call() {
                             ExoPlayer player = players.get(playIndex);
                             if (player.getPlaybackState() == Player.STATE_READY) {
                                 long rawPosition = player.getCurrentPosition();
                                 Log.d(TAG, "Raw position: " + rawPosition);
-                                position[0] = rawPosition / 1000.0;
+                                return rawPosition / 1000.0;
                             }
+                            return 0.0;
                         }
-                    }
+                    },
+                    0.0
                 );
-            return position[0];
+            } catch (Exception e) {
+                Log.e(TAG, "Error getting current position", e);
+            }
         }
         return 0;
     }
@@ -631,6 +675,7 @@ public class RemoteAudioAsset extends AudioAsset {
                             double currentTime = player.getCurrentPosition() / 1000.0; // Get time directly
                             Log.d(TAG, "Timer update: currentTime = " + currentTime);
                             owner.notifyCurrentTime(assetId, currentTime);
+                            owner.updateMediaSessionProgress(currentTime);
                             currentTimeHandler.postDelayed(this, 100);
                             return;
                         }
